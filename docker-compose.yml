services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: koel
      POSTGRES_PASSWORD: koel
      POSTGRES_DB: koel
    volumes:
      - koel_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U koel -d koel"]
      interval: 5s
      timeout: 5s
      retries: 10

  # Optional dash next to Postgres: docker compose --profile web up -d
  web:
    profiles: ["web"]
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://koel:koel@postgres:5432/koel
      DASH_SESSION_SECRET: ${DASH_SESSION_SECRET:-dev-compose-change-me}
      DASH_DEMO_AUTH: ${DASH_DEMO_AUTH:-0}
      DASH_DEMO_TELEGRAM_IDS: ${DASH_DEMO_TELEGRAM_IDS:-}
      DASH_DEFAULT_TELEGRAM_ID: ${DASH_DEFAULT_TELEGRAM_ID:-}
      # Host poller health (loopback on the machine running compose), not in-container.
      HEALTH_URL: ${HEALTH_URL:-}
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  koel_pgdata:
